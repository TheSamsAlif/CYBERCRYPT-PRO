"""
ui package
==========

The premium glassmorphism interface built on CustomTkinter.

    theme.py      -> colours, fonts, spacing (the design system)
    animation.py  -> smooth colour / alpha animations
    background.py -> glass gradient + glow rendering
    widgets.py    -> reusable glass components
    screens/      -> one module per screen (dashboard, encrypt, ...)
"""
